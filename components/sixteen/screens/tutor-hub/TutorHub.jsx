'use client';
import TutorInvite from '../TutorInvite';

// Tutor — the student's tutoring home: chat with your tutor and manage who can
// see your practice.

export default function TutorHub({ go }) {
  return <TutorInvite go={go} />;
}
