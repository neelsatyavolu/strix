# Desktop shell + vendored design reference + local artifacts — not part of the
# Vercel (Next.js) build. Anchored to repo root so they don't match lib/supabase, etc.
/electron
/design-src
/docs
/supabase
/.playwright-mcp
*.png
/SETUP.md
# local build artifacts — Vercel builds fresh, must not receive a standalone .next
/.next
/dist-app
/build
